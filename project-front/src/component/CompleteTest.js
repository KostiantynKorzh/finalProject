import React from "react";
import Header from "./Header";

const CompleteTest = (score) => {

    return (
        <div>
            <h1>{score.props}%</h1>
        </div>
    );

};

export default CompleteTest;